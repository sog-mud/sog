/*
 * $Id: obj_wear_giant_str.mpc,v 1.4 2003-07-21 23:46:11 fjoe Exp $
 */

if (is_sn_affected(actor, "giant strength"))
        return 0;
 
act("As you wear $p, you feel your self getting stronger.",
     actor, obj, NULL, TO_CHAR);
act_char("Your muscles seems incredibly huge.", actor);
 
int mod = 1 + (actor->level >= 18) + (actor->level >= 30) + (actor->level >= 45);
affect_char(actor, TO_AFFECTS, "giant strength", actor->level, -2, APPLY_STR, mod, 0);
return 0;
