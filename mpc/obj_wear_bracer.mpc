/*
 * $Id: obj_wear_bracer.mpc,v 1.4 2003-07-21 23:46:11 fjoe Exp $
 */

if (is_sn_affected(actor, "haste"))
        return 0;

act("As you slide your arms into $p, they mold to your skin.",
    actor, obj, NULL, TO_CHAR);
act_char("Your hands and arms feel incredibly light.", actor);

int mod = 1 + (actor->level >= 18) + (actor->level >= 30) + (actor->level >= 45); 
affect_char(actor, TO_AFFECTS, "haste", actor->level, -2, APPLY_DEX, mod, AFF_HASTE);
return 0;
