/*
 * $Id: mob_random_nasty.mpc,v 1.2 2002-11-23 15:39:38 fjoe Exp $
 */

char victim;
foreach (victim, char_in_room(char_room($n))) {
        if (!is_npc(victim)
        &&  char_level(victim) > char_level($n) - 5
        &&  char_level(victim) < char_level($n) + 5
        &&  !is_safe_nomessage($n, victim)
        &&  can_backstab($n)) {
                backstab_char($n, victim);
                return 1;
        }
}
 
return 0;
